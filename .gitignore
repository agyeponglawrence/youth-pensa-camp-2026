# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editor cruft
*.swp
*~

# Local backups made while editing
*.bak
index.html.bak

# The audio the page actually serves lives in audio/, named by page id.
# "Audio files/" is the original drop folder those were copied from —
# ignored so the same ~59 MB is never committed twice.
Audio files/
